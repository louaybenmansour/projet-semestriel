# Données

- **`student_performance_factors.csv`** : jeu brut (facteurs de performance, `Exam_Score`).
- Les fichiers préparés pour la modélisation sont générés dans **`outputs/processed/`** après `python scripts/run_pipeline.py`.

Source : placer ici le CSV du projet « Student Performance Factors » (même nom que dans `src/config.py` : `RAW_DATA_PATH`).
